# SPT Spot compatibility audit — APP-3A

Status: reviewed source and isolated capture only; **whole indicator is not yet Spot-clean or execution-ready**. Baseline is the owner-supplied Pine v6 indicator with SHA-256 `2f353c49721c8daf24e366409eaf6eac49f945da955a0df8bd52862f2e7e3277`. The similarly named legacy file under `tradingview/` has a different hash and is **not** this audit's baseline. Do not replace the source of the active TradingView capture alert during parity collection.

## Findings

| Area | Source evidence | Spot assessment |
| --- | --- | --- |
| Market data | `request.security(syminfo.tickerid, ...)` at lines 715–743; no hard-coded futures ticker or external contract feed in the signal path. | The signals use the chart's instrument. The observed alert chart is BINANCE:BTCUSDT Spot 1m. A ticker-only runtime guard could accept a different venue with the same ticker; new `bridge-exit-v2` artifacts require exact `BINANCE:BTCUSDT` ticker ID for `binance-global`. Existing v1 alerts retain their frozen weaker guard and remain capture-only. |
| BUY signal | `allowLong`, confirmed long setup and long-risk preview feed `rawBuySignal`/`buySignal` at lines 839–1003. | Valid as a candidate funded Spot BUY on the verified Spot chart. SPT's preview SL/RR and optional simulated position are separate from Bridge ATR multiplier 2.0 / RR 1.5 and Bot Risk Manager allocation. |
| Opposite signal | `allowShort`, bearish setup, short SL preview and `validShortRisk` feed `sellSignal` at lines 845–1003. | This is a **short-entry condition**, not an inventory-aware Spot sell. Owner-approved Bridge mapping uses it only as an EXIT intent for existing longs. Keep source direction `Both Long & Short`; `Long only` disables this exit signal. A bearish move can occur without `sellSignal`, and an unheld sellSignal has no Spot order. |
| Signal memory | `newShortSetup` clears `longSetupActive` at lines 859–864. The short visual branch updates `tradeStartBar` at lines 1358–1367; `cooldownOK` reads that variable at lines 970–975. | Short-oriented state can change later BUY eligibility even though the Bridge never opens a short. Removing or disabling short branches is therefore a signal change and requires a separate approved source revision plus fresh parity evidence. |
| Simulated dashboard | `btOpenShort` sets `btPos=-1` at lines 1123–1155; short SL/TP, reverse and win-rate paths run at lines 1053–1175. The source has no explicit fee/slippage/funding model here. | The default mixed long/short dashboard is **not Spot Bot or Quant Lab performance**. Its R and win-rate metrics must not enter Quant optimization or the Email Report. A Spot adaptation must remove/relabel these calculations or provide a separately defined long-only simulation with Spot costs. |
| Visual trade | `sellSignal` creates `Short active`, downward TP levels and short SL at lines 1358–1415. Later TP/SL checks branch on direction at lines 1426–1515. | Chart labels and short TP/SL are misleading for Spot execution. A reviewed Spot revision should show `EXIT long` only when a long is represented, without creating a synthetic short visual position. Since this branch affects cooldown, the revision needs measured BUY/EXIT deltas. |
| Native notifications | `f_eventSide("SELL")` returns `SHORT` at lines 1548–1560; `f_sendNotify` calls `alert()` when `notifyEnabled` at lines 1707–1727. | Keep `Enable notifications=false` in every Bridge/trace alert snapshot. Native `Any alert()` payloads are not Bridge v1 messages. The existing staging configuration has this input off; any change needs a new isolation check. |
| Leverage/contract economics | No executable leverage, margin, liquidation, funding, contract quantity or borrowing logic was found in the submitted source's signal/notification path. | No direct Futures execution call was found. This absence does not make the short simulation Spot compatible or establish Quant evaluator support. |

## Decision and next revision

The current APP-3A artifact may continue **capture-only evidence collection** on the verified Spot chart. The Bridge restricts execution semantics to funded long BUY and targeted reduce-only EXIT; actual Spot fills, quantities and costs remain server-owned. Do **not** mark the whole SPT indicator Spot-adapted, Webhook ready or Quant supported from this source audit.

For a Spot-clean indicator revision, preserve the bearish detection calculation as an explicit exit candidate; replace short-entry wording, visual position and dashboard paths with long-only Spot semantics; define whether a bearish signal while flat still starts cooldown; and freeze Bot/Quant cost and position rules separately from SPT preview metrics. Show the complete line-level source diff and expected behavior changes to the owner before registering that new immutable source hash. Compile and compare its signals against its own frozen baseline; do not transfer parity evidence from the unchanged source automatically.

The exact Binance Spot ticker-ID check was added to future `bridge-exit-v2` generation. This changes the generated artifact version, not the frozen existing TradingView capture alert or its source hash. Other brokers need verified chart-ID mappings before claiming the same venue guard.

## Spot review draft (not registered)

`scripts/build-spt-spot-review.mjs` creates a private, reproducible draft from the exact owner baseline hash above. The current draft is `.qa-local/spt-spot-review-v3.pine`, SHA-256 `c743df4b5c1b8b14ad51317632df1cf3fd06d18346d7143f440dab92b4c6b799`. It has **not** replaced the active source, Bridge alert or Backend registration.

| Change | Expected signal effect |
| --- | --- |
| Rename default `Both Long & Short` to `Long + Exit`, retaining both allow flags and the original bearish setup/risk calculations. | Default BUY and bearish exit conditions remain enabled. |
| Replace simulated short opens, short SL/TP and short win-rate dashboard with an indicative long-only simulation. | No direct effect on the signal formulas. These new indicative metrics are not Quant or Bot results. |
| Replace the synthetic short visual position with an EXIT candidate. Preserve `tradeStartBar := bar_index` and setup clearing on `sellSignal`. | Preserve the original cooldown effect on later BUY bars. A bearish signal while flat remains an exit candidate; Bot must ignore it without a held long. |
| Rename the SELL alert side/labels to `EXIT_LONG` and remove short visual TP/SL branches. | No short execution intent is emitted by this draft's native notifications. |

Static source comparison: the 41-line block from `FINAL BUY / SELL SIGNALS` to `BACKTEST ENGINE INSIDE INDICATOR` is byte-identical after the intended `allowShort` → `allowExitSignal` rename. The only assignments to `tradeStartBar` remain the BUY and bearish signal branches. This establishes source-level preservation of those formulas, **not measured TradingView signal parity**. Compile the draft on the verified Binance Spot chart and compare closed-bar BUY/EXIT traces with the frozen baseline before registering or replacing any alert. Keep native `Enable notifications=false` in the review chart/alerts; the Bridge is the execution payload source.

### TradingView comparison, 2026-09-25

The owner placed the full 2,353-line draft in TradingView. It compiled and ran on `BINANCE:BTCUSDT` 1m; the private saved script is `SPT PRO Spot review APP-3A DRAFT`. Existing capture alerts were not changed. TradingView's chart-data CSV export contained the Spot draft, original indicator, and original native-trace indicator on the same chart with the displayed default signal inputs. The open final candle was excluded.

| Closed UTC bar window | Bars | Original BUY / Spot BUY | Original SELL / Spot EXIT candidate | Flag mismatches |
| --- | ---: | ---: | ---: | ---: |
| 2026-09-21 22:37–2026-09-24 18:11, 1m, no gaps | 4,055 | 56 / 56 | 0 / 0 | BUY 0, EXIT 0 |

The expanded export has SHA-256 `c49340c1f3db323e5fb5ae4e33a5ace5359c419c9c7c03299dac4dd11c1381af` and supersedes the 749-bar export. The native-trace BUY/EXIT plots and the second original indicator also matched these 4,055 closed bars. This supports BUY parity for this window. Because there was **no positive bearish exit signal**, it does not establish observed EXIT parity or the full APP-3A parity gate. The Spot draft remains review-only. Quant and Bot performance must still be measured separately from SPT's indicative dashboard.

### HTF-off Spot candidate, 2026-09-25

An evidence-only SPT trace with 21 diagnostic plots isolated the zero-EXIT cause on the one-minute Spot chart. With the original confirmed-HTF filter enabled, `shortBias` was true on 0 of 1,610 closed exported bars, although the sell zone was touched on 159 bars. Disabling only `Use confirmed HTF filter` on the diagnostic chart produced 29 native EXIT candidates and 26 BUY signals over those same bars, versus 0 EXIT and 27 BUY with the filter enabled. This changes the effective signal baseline; old alert/parity evidence cannot be carried forward.

The owner selected the HTF-off approach. The private Spot candidate changes only the `useMTF` input default from `true` to `false` relative to the review source (candidate SHA-256 `0be2c64c85ea2c7ef15b00b3bc1d73df1b9ee140398ef2a23a7858209999f01a`; review source SHA-256 `c743df4b5c1b8b14ad51317632df1cf3fd06d18346d7143f440dab92b4c6b799`). It compiled and ran on the standard BINANCE:BTCUSDT one-minute chart. Native source notifications were disabled on that chart instance. Across 1,622 closed exported bars, this Spot candidate and the HTF-off SPT diagnostic matched on all BUY and EXIT flags: 26 BUY, 29 EXIT, 0 mismatched bars for either flag. The comparison is historical chart evidence, not point-in-time repaint evidence or live webhook parity. At the time of that export, this candidate had not been registered or activated for Bot execution. The later expanded 5,524-bar window supersedes these initial sample counts; initialization and evaluator parity remain open.

Correction at 2026-09-25 23:06 Asia/Bangkok: session `fb3d058a-7e39-491d-a511-edc5d7779f9f` was previously mislabeled HTF-off. Reopening the native-trace chart Inputs showed the filter still enabled. Retain its data as unverified-input transport evidence; exclude it from HTF-off parity/repaint. The earlier four-bar rotation gap remains a gap. After saving and reopening Inputs to confirm HTF=false and notifications=false, checking the native-trace source in Pine Editor, and creating a new alert directly from that instance, verified collection begins with session `22e5c53b-c7b2-4fac-b186-78ece3ea52d1` at close `1790352360000`. The new alert is `SPT HTF OFF verified 1m CAPTURE ONLY`; both superseded 1m SPT alerts are stopped. Backend registration remains the original DRAFT, so this is an explicitly overridden evidence baseline, not authorization for execution or a registered Spot candidate.

### Expanded HTF-off comparison, 2026-09-25 23:10 Asia/Bangkok

TradingView export SHA-256 `f1485a85890b620a78a8752dee565b6dfcbb2f1e922fc7ae1a70bf470ab16d38` covers candle-open UTC 2026-09-21 20:06 through 2026-09-25 16:09. Excluding the final open candle leaves 5,524 consecutive closed bars, 75 BUY and 99 EXIT. The HTF-off Spot candidate, diagnostic original and native-trace flags agree on every bar. Discarding a provisional 1,000-bar warm-up leaves **4,524 bars, 65 BUY, 79 EXIT, zero flag mismatches and zero gaps**. This exceeds the numerical count floors for that provisional window, but does not certify initialization or Quant evaluator parity. The warm-up must be `max(500, 5*L)` using the largest lookback across the actual optimization bounds; EMA(200) establishes only a default lower bound of 1,000. Stateful initialization remains to be recorded/reviewed.

Five newly recorded live trace observations matched the later historical native flags after joining candle-open CSV time plus 60 seconds to payload `bar_time`; **0 changed flags**, but all five were non-event bars. This is only 5/100 repaint observations and does not establish signal-event stability. Private reproducible analysis and results are `.qa-local/compare-spt-evidence.mjs` and `.qa-local/spt-evidence-comparison.json`; the CSV and source remain private. Historical observations satisfy historical sample counting only; never relabel them as webhook observations. A 42-hour wait is not a readiness guarantee. Next work: complete the 100-observation comparison, freeze full effective inputs/numeric bounds and initialization, then review/register the Spot revision and perform its own required Bridge/evaluator checks before activation.

### Live versus recomputed history, 2026-09-26 01:03 Asia/Bangkok

Session `22e5c53b-c7b2-4fac-b186-78ece3ea52d1` reached 118 recorded closed-bar observations (close timestamps `1790352360000` through `1790359380000`), with 0 BUY and 3 native EXIT candidates. The chart was switched away from 1m and back to recalculate historical flags, without changing the server alert. The subsequent CSV (SHA-256 `919a91a1c8708359867c942b9583ea6c15e602a6fd245acbf1590fec2cffc4f6`) matched **118/118 observations, zero changed BUY/EXIT flags**, including all 3 EXIT events. The HTF-off Spot candidate flags also matched on those 118 bars. The open final CSV candle was excluded; CSV candle-open time plus 60 seconds was joined to payload close time. Private results are `.qa-local/spt-repaint-20260926.json`.

This meets the minimum 100-observation zero-change repaint check for this observed baseline/window. It does not prove universal non-repainting, live BUY stability (no BUY occurred), Quant evaluator parity, or registration of the Spot candidate. The immediately preceding 117-bar transport audit found no gaps, 117/117 market bars frozen before receipt, zero rejections, zero close mismatches and zero execution events. Keep collecting without resetting the session; remaining work is effective-input/bounds and initialization review plus the candidate's separate registration/readiness checks. This result supersedes the earlier 5/100 repaint count only.

### Effective inputs and registration boundary, 2026-09-26

Read-only inspection of staging deployment `00ccc140-0fac-4bde-91ca-52c1c3163875` confirms it is still DRAFT with registered **original source hash** `2f353c49721c8daf24e366409eaf6eac49f945da955a0df8bd52862f2e7e3277` and snapshot hash `ea98c0e12e1c7a4c4b9a2c9b836115f374d6bbc193dfd10fb9aed9283eec1de2`. The stored fixed inputs include `preset=Daily/Swing`, `tradeDirectionectionection=Both Long & Short`, `useMTF=true`, `notifyEnabled=false`; Bridge slots are ATR multiplier 2.0 and RR 1.5, with zero dynamic bindings. The current trace alert explicitly overrides `useMTF=false`. The reviewed Spot candidate source has hash `0be2c64c85ea2c7ef15b00b3bc1d73df1b9ee140398ef2a23a7858209999f01a` and default `useMTF=false`, but its chart instance explicitly disables native notifications. **Its evidence cannot be attached to the old deployment snapshot or used to activate that deployment.** The candidate needed a distinct analyzed source revision and generated artifact with both effective overrides recorded, followed by its own compilation/Bridge checks. The private line-level source diff is `.qa-local/spt-spot-revision.diff` (78 inserted and 217 removed lines versus the registered original). The owner-approved revision was registered in staging as described below; the original active capture was inspected after the membership change.

For the observed `Daily/Swing` 1m baseline, the source computes EMA fast 50, EMA slow 200, ATR 14, SuperTrend factor 3.2, entry-zone ATR multiplier 1.15, source SL ATR buffer 0.60 and minimum source risk 0.50 ATR. Setup expiry is 48 bars and cooldown is 8 bars from the timeframe mapping; the displayed Custom expiry 8 and cooldown 3 are inactive. `confirmLookback=5`, `pivotLen=5` and RSI length 14 are fixed; RSI, BOS and sweep filters are off. `useMTF=false` makes the selected HTF mode and mappings inactive for signal gating. Source TP RR1–RR5 (1, 2, 3, 4, 5) serve source visuals/indicative backtest; they are not Bridge RR. Source preview SL/risk values still gate native BUY/EXIT, while Bridge 2.0/1.5 set execution protection separately. Visual line projection and dashboard controls are excluded from optimization. With zero dynamic source slots, the largest declared active lookback for this fixed setting is EMA 200, so `max(500,5*200)=1,000` warm-up bars. Changing to `preset=Custom` or selecting any source lookback for optimization requires new finite bounds and a recomputed warm-up. Stateful EMA, SuperTrend, setup/cooldown and pivot initialization must still be recorded before the historical sample is accepted as Quant evaluator evidence.

### Approved Spot revision registered in staging, 2026-09-26

The owner reviewed `.qa-local/spt-spot-revision.diff` and approved this exact Spot candidate. It was registered as source version **4** under the existing Pine import and analyzed through the configured Gemini 3.1 Pro API. The analysis succeeded on the first attempt: 58 source inputs, 14 provider-eligible numeric candidates, source hash `0be2c64c85ea2c7ef15b00b3bc1d73df1b9ee140398ef2a23a7858209999f01a`, 21,696 input / 393 output tokens and USD 0.048108 reported cost. The provider selected `buySignal` for BUY and `sellSignal` for long EXIT. No dynamic source slots were selected for this engineering DRAFT.

The generation job also succeeded on its first attempt: 21,721 input / 520 output tokens, USD 0.049682 reported cost. New deployment `6bf6babf-ffa3-4df9-af9c-d6277b3b1122` is **DRAFT** with snapshot hash `d61ee4bcbab344c3b547408e70cce9167bbacb79955345fcd14ee7bff2ad59ff`, artifact hash `4062f7a39e15ee9f92f1bbe7ab742098e9deb4823be0ffa9c6f6b3571b71867d` and exact unchanged approved source prefix (58,477 bytes). Its frozen inputs are `preset=Daily/Swing`, `tradeDirectionectionection=Long + Exit`, `useMTF=false`, `notifyEnabled=false`, Bridge ATR multiplier 2.0, RR 1.5. The complete integrated Pine is private at `.qa-local/spt-spot-v4-integrated.pine`. The prior capture session remained open after registration (144 received, 0 rejected at the check); it still belongs to the original DRAFT and cannot become evidence for the new deployment by changing its label.

The new integrated Pine has the exact `BINANCE:BTCUSDT` standard-chart/1m guard, `bridge-exit-v2` payload, independent Bridge ATR/RR inputs and Spot long BUY / targeted EXIT mapping. TradingView compiled the complete 2,395-line integrated file and added it to the standard Binance Spot 1m chart. The script was saved privately in TradingView as `SPT APP-3A Spot Bridge DRAFT v4`. Its chart Inputs were saved and reopened: `preset=Daily/Swing`, `Spot Signal Mode=Long + Exit`, `Use confirmed HTF filter=false`, `Enable notifications=false`, Bridge ATR multiplier 2 and RR 1.5. This is the same effective baseline as the new deployment snapshot. Its source default for native notifications is true, so another new chart instance must explicitly disable them as well. A separate seven-day capture-only session for this DRAFT was created in staging (`292cfaef-3f9a-42c8-a862-d28ea2396f40`, `execution_enabled=false`); the old capture remained open. With the owner's explicit authorization to send the capture URL/token to TradingView, an Alert was created from this specific indicator on the 1m chart using `Any alert() function call`. The webhook URL was saved and reopened for confirmation, and `SPT Spot Bridge v4 STAGING CAPTURE ONLY 1m` appeared Active. The first read-only staging check, before the BUY reported below, showed 0 received, 0 duplicate and 0 rejected events for the new session; the old trace session showed 160 received and 0 rejected. No Bridge execution events were present. No Paper admission or deployment activation has been enabled. Quant remains `UNSUPPORTED` for this source until its evaluator handles `request.security` and pivot state and passes its own evidence gates. The two successful Gemini jobs report USD **0.097790** combined; earlier ambiguous requests are separate.

The first new-snapshot live signal arrived at 2026-09-26 02:12 Asia/Bangkok (bar close `1790363520000`). The `bridge-exit-v2` event type was `BUY`, source version 4, close 83,994 and ATR 25.733841876831146. Session counters were 1 accepted, 1 duplicate and 0 rejected; the duplicate did not create another event. The parallel verified HTF-off native trace recorded `native_buy=true`, `native_exit=false` and close 83,994 on the same closed bar, 31 ms before the Bridge event receipt. The frozen market row was present at intake; its close and content hash matched, and its retrieval timestamp preceded Bridge webhook receipt by 2,787 ms. The deployment remained DRAFT with 0 Bridge execution events. This is one positive BUY transport/parity observation for the new artifact, not an EXIT check or a Quant readiness result. No Paper admission or deployment activation was enabled.

### Spot v4 protection-exit evidence, 2026-09-26

The subsequent read-only audit found 22 accepted events: **11 BUY and 11 EXIT, comprising 8 SL and 3 TP**, plus 21 duplicate deliveries and 0 rejections. All BUY flags matched the parallel HTF-off native trace. Each EXIT referenced a captured BUY; the shared backend Bridge contract reproduced all 11 reported reasons using entry-close/ATR, multiplier 2.0, RR 1.5, tick rounding and frozen exit-bar high/low. All 22 recorded intake market hashes matched the frozen rows and were present at intake. The deployment remained DRAFT with 0 Bridge execution events. No EXIT had reason `NATIVE`, so these protection events do not prove the live opposite-signal-as-long-exit path. Full intervening-bar first-touch review, evaluator parity and actual Paper execution remain separate gates.

### Complete captured-entry first-touch audit, 2026-09-26

The read-only audit at 05:29:01 UTC examined every frozen 1m candle between the 11 captured BUY and 11 EXIT pairs for v4. All exits occurred at the first SL/TP touch and matched the captured reason; interval gaps, missing BUY references, earlier protection touches, close/ATR mismatches and rounded-level differences were 0. All 22 intake market hashes matched. The auxiliary original HTF-off native trace covered those intervals with no missing observations or earlier native EXIT flags; it is still not a v4 trace snapshot. The deployment remained DRAFT with 0 Bridge execution events. Private results are `.qa-local/spt-v4-first-touch-result.json`. This closes the earlier intervening-candle audit gap for these captured entries only. The separate [acceptance checkpoint](APP_3A_ACCEPTANCE_2026-09-26.md) records v2 controlled Paper routing and timing evidence without claiming natural SPT NATIVE delivery or Quant readiness.

### Historical replay of SELL-to-long-EXIT, 2026-09-26

The owner requested a replay to assess whether waiting for a live `NATIVE` EXIT is productive. The existing CSV with SHA-256 `f1485a85890b620a78a8752dee565b6dfcbb2f1e922fc7ae1a70bf470ab16d38` supplies the approved Spot candidate's historical BUY/SELL flags and OHLC. The final open bar was excluded. The replay used the shared backend `levels`/`exitDecision` contract with ATR(14), multiplier 2.0, RR 1.5, 0.01 tick rounding, next-bar protection, exits before BUY and SL/TP/NATIVE priority. ATR reconstructed from OHLC was cross-checked against exported entry-zone widths divided by the active 1.15 multiplier: after the provisional 1,000-bar warm-up, no entry SL/TP tick levels differed, and both methods produced the same event sequence.

In the **4,524 measured closed bars**, there were **79 native SELL flags and 0 NATIVE Bridge exits**. Every SELL occurred with no hypothetical Bridge long remaining. Replaying the full available prefix before measuring produced 63 BUY entries and 65 exits (43 SL, 22 TP); two exits belonged to entries before the measured window. Resetting Bridge arrays at the measurement boundary instead produced 64 BUY and 64 exits (41 SL, 23 TP), but still zero NATIVE exits and no open long on all 79 SELL bars. This boundary difference is recorded rather than treating either initialization as certified. Results and the reproducible diagnostic are private at `.qa-local/spt-native-exit-replay.json` and `.qa-local/replay-spt-native-exits.mjs`.

The current Pine code permits `NATIVE` EXIT when a SELL arrives while a tracked long survives SL/TP, but this historical window provides no such opportunity. It does not prove that NATIVE exits are impossible in every future market window. Waiting has no completion guarantee with these inputs; keep the live gate unresolved and use a controlled isolated BUY-then-SELL scenario to verify that route without representing fixture events as natural SPT evidence. This replay uses exported SPT flags rather than an implemented Quant SPT evaluator, and does not certify source initialization, live delivery or Paper fills.

### TradingView effective inputs and warning, 2026-09-25

The owner-private Spot draft on `BINANCE:BTCUSDT` 1m initially inherited the source default `Enable notifications=true`, unlike the active Bridge snapshot. Its chart input was changed to **false** and saved; the resulting 60 TradingView UI controls were compared in order with the active Bridge indicator's 62 controls. Every shared value matched except the intentional mode label/value mapping `Both Long & Short` → `Long + Exit`. The other two controls in the active Bridge are its independent `ATR Multiplier for SL=2.0` and `RR=1.5`; this review-only Spot source has no Bridge block yet. The 58 source input declarations themselves are retained in the draft. This comparison describes the **current chart instance**; loading the saved Pine on a new chart still requires setting `Enable notifications=false` because the source default remains true.

TradingView's alert composer for the Spot draft showed: “Caution! This alert may function differently than expected. This is due to calculations being based on an indicator or strategy that can get repainted.” The composer defaulted to `Any alert() function call` on the 1-minute chart. It was canceled without creating an alert. That warning required point-in-time closed-bar signal observations compared with later historical recomputation under the same effective input snapshot; the 118-observation comparison above supplied that bounded evidence, while the 4,055-bar historical CSV alone could not. An `Any alert()` draft with `Enable notifications=false` would also emit no native SPT alert payload by design.
