# APP-3A implementation record

Status: **APP-3A Bridge engineering acceptance passed in staging; QL-2A may start**. Updated: 2026-09-26. The [acceptance record](APP_3A_ACCEPTANCE_2026-09-26.md) records 24 PostgreSQL and 22 focused Node passes, hosted Paper timing and the deployment-specific manifest. This record supplements [ROADMAP.md](ROADMAP.md) and [PINE_BRIDGE_ADAPTER_API.md](PINE_BRIDGE_ADAPTER_API.md). Owner SPT activation, production rollout and Quant certification are separate gates.

## Implemented

- Optional PostgreSQL extension version 1 on base schema 14: owner/Bot-scoped source registry, immutable revisions, membership, jobs/attempts, deployment snapshots, trusted evidence/market records and event/allocation mappings.
- Authenticated analyze/generate/status/cancel APIs, cookie/CSRF/Origin checks, durable idempotency, queue/concurrency limits, deadline/lease recovery, cancellation and usage accounting. Provider outcomes that may have consumed work are not automatically resent.
- Pine v5/v6 indicator preflight; strategy rejection before provider dispatch; 2 independent Bridge numeric slots plus 0–8 selected source slots. Effective input overrides are typed and recorded. Original source bytes remain unchanged; effective settings must be applied in TradingView and reviewed.
- Direct AI API with versioned prompt/guide. AI proposes identifiers and eligibility; backend validates those proposals and renders the reviewed template. No MCP connection or AI-generated executable block enters the backend.
- Private draft UI with dropdown mapping, status/cancel, Pine/manifest/guide downloads and logout cleanup. Membership/revision/activation operations are API capabilities; the UI remains a draft builder.
- Scoped `bridge-exit-v1` receiver and Paper worker. Accepted BUY fills atomically acquire entry/allocation mappings; rejected BUY events acquire none. EXIT resolves the exact open allocation. Duplicates are idempotent; changed payloads with the same event ID conflict. Legacy webhook behavior remains separate.
- Independently collected closed OHLCV and ATR(14), frozen entry-close levels, tick/quantity rounding, recorded fee/slippage model, next-bar protection, SL-before-TP-before-native priority and BUY suppression on exit bars. Missing trusted data or evidence blocks execution.
- Snapshot checks at activation, receipt and queued BUY execution. Source/membership changes invalidate entry routes. Policy/funding changes invalidate BUY freshness. Replacement keeps old deployments EXIT_ONLY, allowing scoped exits. Creating an independent draft does not replace an active deployment until activation.
- Operator-recorded evidence plus owner activation. Runtime DB privileges cannot write evidence or verified bars. Activation requires one connected Pine; multi-Pine activation remains blocked until APP-3B. Activation does not start a Bot or confer Quant support.

The scanner is structural, not a Pine compiler. AI eligibility is a proposal, not proof of signal dependencies. Every deployment starts DRAFT; compilation and source-specific review must be recorded separately.

## Configuration and operation

Feature defaults off (`PINE_BRIDGE_ENABLED=0`). Production application services, database and execution alerts were not changed. The public nginx configuration gained the isolated capture route described below. Gemini requests used the owner's privately supplied key after Billing was enabled. Successful usage and reported cost are recorded below; earlier ambiguous requests have no returned usage. No email was sent. Tests and staging use a separate temporary PostgreSQL cluster and private Unix socket; production credentials and data were not used.

The optional extension is separate from schema 14. Before rollout, take a protected backup, stop API/workers under the maintenance lock, run `node --env-file-if-exists=.env scripts/migrate-pine-bridge.mjs`, and reapply `scripts/grant-postgres-runtime.sql`. Never migrate automatically on service startup. The isolated migration rollback and backup/restore rehearsal passed; this is not a production migration claim.

The `openai-chat` adapter allowlists `gpt-4.1`, `gpt-4.1-mini`, and `gpt-4.1-nano`. The `gemini` adapter allowlists `gemini-3.8-flash` and `gemini-3.1-pro-preview`. Model selection is explicit; there is no automatic fallback. Privately configure `PINE_AI_PROVIDER`, `PINE_AI_MODEL`, `PINE_AI_API_KEY`, both per-million rates and `PINE_AI_RATE_VERSION`. No default pricing is assumed. Other models/providers need a compatible accounting adapter. OpenAI counts full source and compact mapping metadata with pinned `js-tiktoken@1.0.21` / `o200k_base`, plus a 1,024-token framing reserve. Gemini reserves the full input cap and calls Google's `countTokens` before generation. Its versioned AI-only view removes comments and blank lines while retaining code, literals and indentation; stored source and assembled Pine retain the original bytes. Gemini thinking tokens count toward output usage and cost. Limits remain 24,000 input / 4,000 output per attempt, two attempts only for explicit rate refusal, 56,000 reserved tokens and USD 0.50 per job. Polling performs no provider calls. See [tokenizer documentation](https://www.npmjs.com/package/js-tiktoken), [Chat API](https://developers.openai.com/api/reference/resources/chat), [Gemini tokens](https://ai.google.dev/api/tokens) and [Gemini pricing](https://ai.google.dev/gemini-api/docs/pricing).

### Isolated capture and provider progress (2026-09-24)

- An optional capture extension, version 1, is separate from both base schema 14 and Bridge extension 1. Install offline with `scripts/migrate-pine-capture.mjs`, then reapply runtime grants. Startup requires `PINE_BRIDGE_ENABLED=1`, `PINE_BRIDGE_CAPTURE_ENABLED=1` and `PINE_BRIDGE_ENV=staging`.
- Owner-authenticated capture creation/status/close routes issue an expiring token for a DRAFT deployment. Only the token hash is stored. Tokens rotate on replacement; sessions have bounded lifetime and event counts. Invalid native payloads increment rejection diagnostics without storing unexpected bodies. Capture never enqueues Paper trades, writes readiness evidence or activates a deployment.
- Isolated staging API and worker run on loopback with a separate database and restricted runtime role. Email is disabled. The PostgreSQL cluster is temporary; this setup is not a persistent production rollout. The VPS operator installed a restricted public HTTPS nginx route for `/webhooks/pine-capture/v1/` only, proxied to the staging loopback API. The route accepts POST only, uses webhook rate limiting and disables access logging because the URL contains a bearer token. A POST with an invalid token reached the staging receiver and returned JSON `NOT_FOUND`. No production execution route or service was reconfigured.
- The original SPT request exceeded the Gemini input cap and failed before generation. After adding the AI-only view, a new SPT analysis reached `OUTCOME_UNKNOWN`; no result from that attempt was accepted. A separate small Flash connectivity probe returned HTTP 503 on generation. These are distinct observations, not proof of the SPT request's HTTP status.
- The owner enabled Billing. After an explicit staging switch to Gemini 3.1 Pro, a small connectivity request succeeded. A fresh SPT analysis succeeded with 22,885 input and 553 output tokens, observed provider cost USD 0.052406. The first generation attempt failed its preflight token count because frozen inputs were duplicated in metadata; it made no generation call. Compacting only the repeated selection metadata allowed a fresh generation job to succeed with 22,910 input and 484 output tokens, observed provider cost USD 0.051628. Total reported successful AI usage cost: USD 0.104034. Earlier unknown/503 attempts have no returned usage; their billing status must be checked at the provider.
- The actual SPT AI draft has source hash `2f353c49721c8daf24e366409eaf6eac49f945da955a0df8bd52862f2e7e3277`, artifact hash `0dee46eb6bb7cdbeb6a15d4abab4b0a1f5d41c44c2ad1c6ec051ce9fb33130ea` and zero changed original bytes. It is DRAFT with two Bridge slots and zero selected source slots. A capture-only session exists; its token is held privately and expires after seven days.
- The 2,534-line draft compiled and appeared on a standard BINANCE:BTCUSDT 1D TradingView chart. The owner-private script was saved as `SPT APP-3A staging draft 2026-09-24`. In its Inputs dialog, `Enable notifications` was set to off and the independent Bridge inputs displayed 2 and 1.5; `Daily/Swing` and the other source settings remained at defaults. After saving, the chart dashboard displayed `Notify OFF`. The active TradingView alert `SPT APP-3A staging CAPTURE ONLY 1D` uses that indicator's `Any alert() function call` on BTCUSDT 1D, sends only to the private staging capture URL, and expires 2026-10-01 21:30 Asia/Bangkok, before the capture token. TradingView displayed a repaint caution. No captured event has been observed for the 1D SPT alert; its status alone does not prove delivery, signal parity or Quant support. Capture cannot execute Paper trades.
- A chart interval change to 10 minutes caused the 1D registered Bridge to raise `RE10142` at bar 0 as designed. The existing TradingView alert list still showed the 1D capture alert as Active on BTCUSDT 1D; chart display and alert snapshots have separate intervals. For a faster SPT signal check, a second DRAFT was generated from the same unchanged source and analyzed input snapshot for BINANCE:BTCUSDT 1 minute. Its artifact hash is `cc22a9a0307332cfc3619108b74b768923fa1734b9f9edde917c86bc5c2fc007`, original bytes changed 0, and the new Gemini generation reported 22,910 input / 474 output tokens and USD 0.051508. It retains the `Daily/Swing` preset and Bridge 2/1.5 inputs; `Enable notifications=false` was explicitly set in TradingView. The script compiled and displayed on the 1-minute chart without that runtime error. The separate webhook-only alert `SPT APP-3A staging CAPTURE ONLY 1m` became Active on BTCUSDT 1m and expires 2026-09-25 21:30 Asia/Bangkok, before its capture token. Its capture session is separate from the 1D session.
- The 1-minute **actual SPT** alert delivered a BUY for closed bar `1790266560000`, then an EXIT with reason `TP` for the next closed bar `1790266620000`, targeting the same `entry_ref`. The isolated capture recorded **2 unique events, 2 duplicate deliveries, 0 rejected**, and **0 Bridge execution events**. Duplicate counts establish idempotent capture, but their transport cause has not been determined. Receipt lags from bar close were 7,815 ms and 5,975 ms. The BUY close was 84,449.36, identical to the frozen Binance bar; captured ATR(14) was 79.32957392069474 versus frozen market ATR 79.329573920695792921 (relative difference about 1.3e-14). With Bridge ATR multiplier 2.0, RR 1.5 and tick 0.01, the frozen SL/TP levels are 84,290.70/84,687.35. The next frozen bar's high/low were 84,750.00/84,440.01, consistent with TP and no SL touch. This validates these two webhook events and their market-bar comparison only; source signal mapping, repaint behavior, a quantitative multi-bar parity sample and Paper execution readiness remain open.
- A separate deterministic transport fixture used its own staging owner, DRAFT deployment, 1-minute BINANCE:BTCUSDT indicator and capture session. Its original source bytes were unchanged by Bridge assembly. TradingView compiled the fixture and ran a webhook-only alert. The staging receiver recorded a real BUY at bar close `1790262780000` and a `NATIVE` EXIT at `1790262840000` targeting that BUY's exact `entry_ref`. Replaying the captured BUY once through public HTTPS returned `duplicate=true`: final fixture counters were **2 received, 1 duplicate, 0 rejected**, with **0 Bridge execution events**. The fixture alert was then stopped manually and its capture session closed. Two earlier real-Gemini fixture analysis attempts ended `OUTCOME_UNKNOWN`; the fixture used a deterministic provider only for transport verification. These fixture results prove the external capture path and scoped event structure, not SPT signal parity or Paper execution readiness.
- The staging database received 499 closed BTCUSDT 1D bars from Binance with a 500-bar ATR warmup. Seed hash: `0051b45e1bce32d41402308443947b87c745570d448796037b27477fae1a3c92`. A bar-by-bar TradingView comparison and automated collection schedule remain open.
- The same isolated collector received 499 closed BTCUSDT 1-minute bars with a 500-bar warmup for the rapid test. The later incremental load used seed hash `688ff4a47a34d6502c799d230eef3068c7952de7e3e031dd719fbb514cfa22de`, retained previously frozen bars and covered the actual SPT events above. A one-time load does not establish scheduled intake or broad parity against TradingView.
- Provider secrets remain in ignored/private configuration and are excluded from deployment source archives and documentation. The pending actual SPT configuration keeps the original preset and all effective inputs, except `notifyEnabled=false` for native-alert isolation. It selects zero dynamic slots for the engineering check; no optimization list is inferred for the owner.

Trusted market ingestion is a separate operator process: `node --env-file-if-exists=.env scripts/collect-pine-bars.mjs BTCUSDT 1D`. It requires a restricted market writer identity; never give its write permission to the API/AI role. The initial collector supports Binance Global USDT spot and explicitly listed intervals, 1,000 contiguous bars and 500 warmup bars. Other markets can produce drafts but require their own verified data adapter before activation. The collector freezes the first bar and rejects changed OHLCV or venue metadata. ATR comparison permits relative floating-point noise up to 1e-10 only when both frozen levels still match exactly at the tick. Collector scheduling and bar arrival before webhook intake remain deployment tasks; absence of a bar rejects intake and must not be represented as successful delivery.

Collection increment: the market writer now resumes from the last frozen close and pages forward in 1,000-bar requests, carrying the previous close and ATR(14) state into each next page. Existing frozen rows are preserved, including the original 499-bar SPT sample. The command reports newly written and total frozen bars. Run it periodically under the restricted writer identity; a missed interval can be caught up on the next run (up to 100 pages per invocation). This is collection code, not evidence that a recurring job has been installed or that 2,000 bars have been observed. The older 1,000-bar bootstrap remains for a fresh market series. Before declaring Quant parity, freeze and audit the complete contiguous sample, its seed/provenance and native signal traces; a count of market bars alone cannot satisfy the gate.

On 2026-09-25 the incremental collector was copied to the isolated staging workspace and run for BINANCE:BTCUSDT 1m: first **67 newly frozen bars, 606 total**, then another 15 to **621 total**. A transient `astra-pine-market-stage.timer` was installed under the staging user to call the restricted collector every 5 minutes. It depends on the temporary staging PostgreSQL cluster and is not a durable production schedule. The capture receiver update was loaded by restarting only `astra-pine-stage-api.service`; production services were not restarted. The evidence-only SPT trace artifact was generated privately from source hash `2f353c49721c8daf24e366409eaf6eac49f945da955a0df8bd52862f2e7e3277` and deployment `00ccc140-0fac-4bde-91ca-52c1c3163875`, with 0 changed source bytes. TradingView compiled the full trace indicator on the same standard BINANCE:BTCUSDT 1m chart, with native `Enable notifications=false`. Its dedicated `Any alert() function call` alert, `SPT APP-3A native trace CAPTURE ONLY 1m`, is Active and expires 2026-09-25 21:30 Asia/Bangkok before the capture token. It sends to the existing staging capture URL and cannot execute Paper orders.

Native parity trace: the Bridge webhook emits BUY/EXIT intents only and cannot prove native BUY and exit flags were false on every other bar. `scripts/build-pine-parity-trace.mjs DEPLOYMENT_ID .qa-local/native-trace.pine` builds a separate evidence-only indicator from the frozen source revision and approved signal mapping of a DRAFT deployment. It appends global BUY/EXIT flag plots for historical chart-data export and an `alert()` on every confirmed bar with `bridge-native-trace-v1` booleans, time and close. Use its own TradingView alert, with the same registered standard Spot chart/timeframe and an isolated capture session; keep the source's native notification input off. The staging capture receiver validates, deduplicates and stores these trace rows without enqueueing Paper trades. The owner status includes trace count and timestamp range; the list of latest events is capped at 100. `scripts/summarize-pine-trace.mjs CAPTURE_ID` reports unique trace bars, BUY/EXIT flags, timestamp gaps, missing frozen bars and close mismatches without claiming parity readiness. At the first audit **3 unique 1m native traces** were stored, with **0 BUY, 0 EXIT, 0 gaps, 0 missing frozen market bars, 0 close mismatches and 0 rejected payloads**; Bridge execution remained 0. This is an intake check, not the 2,000-bar/100-event Quant gate. The capture session expires and accepts at most 10,000 total deliveries; for long collections, create a new session and retain its immutable evidence separately. A historical plot export alone is insufficient for point-in-time repaint evidence. Record at least 100 live closed-bar observations, later compare their flags against a full-history recomputation, and keep the chart input snapshot identical. Never mark Quant ready from market bars or sparse Bridge events alone.

Correction, 2026-09-25 23:06 Asia/Bangkok: the earlier rotation to session `fb3d058a-7e39-491d-a511-edc5d7779f9f` did **not establish an HTF-off snapshot**. Reopening the actual native-trace chart Inputs showed `Use confirmed HTF filter=true`; a stale dialog button reference had failed to persist the intended change. Preserve this closed session as unverified-input transport evidence only. Do not count it toward HTF-off parity or repaint. Its earlier four-bar rotation gap is also retained, not filled with synthetic false flags.

The native-trace chart input was then saved with `Use confirmed HTF filter=false` and `Enable notifications=false`, reopened to verify persistence, and its source checked in Pine Editor (2,501 lines, appended `bridge-native-trace-v1` block). A new alert, `SPT HTF OFF verified 1m CAPTURE ONLY`, was created directly from that chart instance. Session `22e5c53b-c7b2-4fac-b186-78ece3ea52d1` expires at `1790956943198` (2026-10-02 approximately 23:02 Asia/Bangkok). Its first close is `1790352360000`. The initial backend check found 4 consecutive traces, 4/4 matching market bars present and retrieved before receipt, 0 rejections, 0 close mismatches and 0 execution events. Both superseded 1m SPT alerts were stopped manually; the separate 1D alert was left unchanged. The backend DRAFT still carries its original registered snapshot: these explicit chart overrides are a separate evidence baseline, not a newly registered Spot deployment.

Historical export on 2026-09-25 23:10 Asia/Bangkok contains 5,524 closed 1m bars; after a provisional 1,000-bar warm-up, 4,524 remain with 65 BUY and 79 EXIT, zero flag mismatches between the HTF-off Spot candidate, diagnostic source and native trace. Five actual live observations overlap the later export with zero changed flags; all five are non-event bars. Detailed hash and limits are in the Spot audit. The Quant requirement is **2,000 compared closed bars after warm-up**, not 2,000 newly collected live bars. Repaint still requires at least 100 recorded/reconstructed point-in-time observations with zero changes. The largest lookback across chosen numeric bounds and state initialization remain uncertified; 1,000 is only the EMA(200) default lower bound. No 42-hour wait guarantees readiness. Do not merge different or unverified snapshots, replace missing observations with false flags, or represent historical chart-to-chart agreement as Quant evaluator parity.

### Market-before-webhook timing audit (2026-09-25)

The original five-minute staging collector did **not** satisfy the arrival-order gate. An audit of 49 native traces found only 14 matching frozen market bars at the time of review; for 13 of those 14, the stored market retrieval timestamp was later than webhook receipt. The collector then stopped advancing because its full Binance `exchangeInfo` hash changed even though BTCUSDT Spot symbol, tick size and quantity step were unchanged. Its metadata fingerprint now uses only those stable trading-identity fields, with a recorded transition from the legacy hash. After a staging-only restart it filled 672 contiguous 1m bars; this catch-up does not establish prior availability.

An isolated transient staging collector timer now runs every five seconds; the earlier five-minute timer is inactive. The capture receiver records `market_present_at_intake`, `market_checked_at` and the visible frozen bar hash for each new valid event. The query executes inside the webhook transaction, so a true value proves that a committed matching market row was visible **during intake**. The market `retrieved_at` timestamp is additionally compared with the request's `received_at`; retrieval preceding receipt alone does not prove the collector committed before receipt. Existing events have NULL audit fields and are excluded from this point-in-time test. The read-only `scripts/summarize-pine-trace.mjs` reports audited true/false counts separately from retrospective market coverage.

The first three audited live 1m trace events showed **2 ready and 1 missing at intake**, with zero rejected captures and zero Bridge execution events. For the ready events, market retrieval preceded receipt by 3,009 ms and 809 ms, respectively, and the frozen hashes matched. For the missing event, retrieval preceded receipt by only 74 ms but the row was not yet visible at intake. Thus the original strict market-before-webhook requirement failed even with five-second polling. A later frozen bar must never turn that missed intake into a pass. The owner then approved the revised before-Paper-admission gate below; the historical intake failure remains in the evidence. This timer and the temporary staging database are not a production collection design.

The owner approved a revised execution gate after this audit: `bridge-exit-v2` may durably acknowledge a webhook as `WAITING_MARKET` while **not yet creating a Paper signal**. The wait expires after at most 5 seconds or at the existing signal-age deadline, whichever comes first. A worker rechecks current Bot/owner state, deployment, snapshot, policy, funding, evidence and the independently frozen closed bar before queuing; a missing/invalid/stale bar rejects the intent. The event ID stays idempotent through waiting, queue and rejection. New Pine drafts emit v2 and use `/webhooks/pine-bridge/v2/`; existing v1 alerts and their fail-fast missing-bar behavior are retained. `scripts/stream-pine-bars.mjs` consumes Binance Spot closed kline events (`x=true`) using the dedicated market writer; REST backfills on startup/reconnect and compares immutable OHLCV, with a slower collector as fallback. Capture-only evidence remains independent and keeps its actual intake-time result, including misses.

The v2 code and `pine_bridge_pending` table were installed only in isolated staging after a staging-file backup and offline migration. The staging API and Paper worker were restored with the same private environment files; both are active and API health returned HTTP 200. The dedicated Spot stream connected and froze consecutive BTCUSDT 1m bars from closed kline events beginning at `1790277600000`. By 19:26 UTC, **7/7** post-stream capture events found the matching bar at intake, versus **0/7** missing; the first three frozen hashes matched and market retrieval preceded webhook receipt by 5,722 ms, 4,454 ms and 2,835 ms. The five-second REST poll is stopped. Its five-minute fallback ran successfully at 19:25 UTC and wrote 0 bars because the stream was current. The live staging database had 0 pending Bridge intents, 0 Bridge execution events and 0 fills; capture-only continued. The API, worker and stream were active with zero restarts. These are initial stream observations, not a v2 Paper wait-path validation or sustained latency result. The capture trace had 98 bars with one gap at the earlier summary, so its separate parity sample remains incomplete. The staging units are transient and depend on the temporary database; durable deployment and longer live observation remain open.

### Paper fixture isolated from capture-only (2026-09-25)

The PostgreSQL Bridge integration fixtures ran on the staging VPS against newly created per-run test databases. An empty fixture-admin database was created first so the test connection did not target the staging database receiving TradingView alerts. Six focused scenarios passed: draft capture/duplicate delivery with no execution; scoped Paper BUY fill and targeted EXIT with duplicate suppression; missing/mismatched market-data and unreviewed-evidence rejection; 10 SL, 10 TP, 5 both-touched and 5 native/Bridge priority cases; 5 rejected plus 5 capped BUY cases; and a separate loopback HTTP request through `/webhooks/pine-bridge/v1/` that queued BUY/EXIT, produced two Paper fills via the execution worker and left `pine_capture_events` at zero in its fixture database. The test harness removed each per-run database. The live staging database still had **0 `pine_bridge_events`, 0 fills and 0 `pine_bridge_entries`** while capture events continued to arrive (78 at the first separation check). This proves the HTTP receiver and worker Paper path with controlled fixture data and confirms isolation from the active capture session. It does not constitute a real TradingView-to-Paper fill or permission to activate the unreviewed SPT DRAFT. The timing, parity and source-review gates above remain open.

Later staging check: the first scheduled market timer run exited successfully, the metadata-aware collector advanced the frozen BTCUSDT 1m set to **625 bars**, and the trace summary reached **6 contiguous native bars**, 0 rejected payloads, 0 close mismatches and 0 execution events. Two newest trace bars were still inside the five-minute ingestion window and were reported as pending, not missing. Neither the 2,000-bar sample nor the 100 native event floor is met.

After real source-specific review, an operator records the complete evidence JSON using `scripts/record-pine-bridge-evidence.mjs DEPLOYMENT_ID REVIEWED_EVIDENCE_JSON`. This binds source, artifact and snapshot hashes, numeric gates, execution model and references. The owner can then POST `{}` to `/api/quant/pine-bridge/deployments/{id}/activate`. A generic READY flag cannot substitute for evidence. Fixture evidence must never be copied into a real deployment.

Sources/jobs currently remain until explicit operator-managed deletion; idempotency exceeds the 24-hour minimum. Customer retention/deletion controls and accumulated-storage quotas remain pending before rollout. Private source and provider bodies are not included in audit errors.

### Repaint observation update, 2026-09-26

The verified HTF-off capture now has a measured **118/118 live-to-recomputed-history match, zero changed flags**, including 3 native EXIT candidates and 0 BUY. This satisfies the 100-observation repaint minimum for that window; see the CSV hash, timing and scope in [SPT_SPOT_COMPATIBILITY.md](SPT_SPOT_COMPATIBILITY.md). The preceding 117-bar backend audit had no gaps or rejected deliveries and matching market data before receipt on every bar. Do not continue waiting solely for the 100-point threshold. The approved Spot revision was subsequently registered with effective inputs bound; TradingView integration, state initialization and Quant evaluator evidence remain separate open gates; this result does not activate the DRAFT or certify APP-3A completion.

The owner subsequently approved the line-level Spot source diff. Staging registered source revision 4 and generated a separate DRAFT deployment `6bf6babf-ffa3-4df9-af9c-d6277b3b1122`, with `useMTF=false`, `notifyEnabled=false`, long BUY/opposite EXIT mapping and Bridge ATR/RR 2.0/1.5 in its own frozen snapshot. Both Gemini jobs succeeded on one attempt, with combined reported cost USD 0.097790. The approved source prefix and artifact hashes matched on export; see [SPT_SPOT_COMPATIBILITY.md](SPT_SPOT_COMPATIBILITY.md) for identifiers and limitations. The original capture session remained open. The new integrated Pine compiled and was added to the standard Spot 1m chart; its Inputs were saved and reopened to confirm HTF off, native notifications off and Bridge 2.0/1.5. The owner authorized sending the new capture URL/token to TradingView. The new `SPT Spot Bridge v4 STAGING CAPTURE ONLY 1m` alert is Active on the new indicator's `Any alert() function call` condition, with the webhook URL saved. The initial staging check, before the BUY reported below, recorded 0 received, 0 duplicate and 0 rejected events. The session has `execution_enabled=false`, the DRAFT remains inactive, and the same check found 0 Bridge execution events. No Paper execution alert was created.

At 2026-09-26 02:12 Asia/Bangkok, the new alert delivered its first real `bridge-exit-v2` **BUY** to the capture-only session. Staging recorded 1 accepted event, 1 identical duplicate and 0 rejected events. Its source version, deployment, BINANCE:BTCUSDT 1m route and closed-bar timestamp matched the new DRAFT. The parallel HTF-off native trace reported `native_buy=true`, `native_exit=false` on the same bar; both reported close 83,994. The frozen market row was present during webhook intake, matched the recorded intake hash and close, and had been retrieved 2,787 ms before receipt. The new deployment still had 0 Bridge execution events. This confirms one live BUY transport and native signal match for the observed snapshot; EXIT delivery, sustained parity, state initialization and Quant evaluator evidence remain open.

### Spot v4 live protection exits, 2026-09-26

A later read-only staging audit found **22 accepted events: 11 BUY and 11 EXIT (8 SL, 3 TP)**, with 21 duplicate deliveries and 0 rejected deliveries. All 11 BUY events matched the parallel native BUY flags. Every EXIT referenced a captured BUY, and the shared Bridge `levels`/`exitDecision` functions reproduced its reported reason from ATR multiplier 2.0, RR 1.5, tick rounding and the frozen exit candle. All 22 events had a matching frozen market hash visible at intake. The deployment remained DRAFT with 0 Bridge execution events. These observations confirm the captured protection-exit path; no `NATIVE` EXIT was captured, so opposite-signal-to-long-exit behavior remains an open live evidence gate. This audit does not establish first-touch timing across every intervening candle, full evaluator parity or Paper fills.

### Historical NATIVE-exit feasibility, 2026-09-26

A replay of the approved Spot candidate's exported historical flags over 4,524 bars after provisional warm-up found 79 native SELL flags, all with no Bridge long remaining, and **0 NATIVE exits** under ATR multiplier 2.0 / RR 1.5. All simulated exits were SL/TP. Reconstructing ATR independently from exported zone widths produced identical entry tick levels and event sequences. Carrying Bridge state from the CSV prefix versus resetting at the measurement boundary changed BUY/protection counts, but both produced zero NATIVE exits; initialization remains an explicit limitation. See [SPT_SPOT_COMPATIBILITY.md](SPT_SPOT_COMPATIBILITY.md) for counts and private reproducibility artifacts. The code permits SELL-to-targeted-long-EXIT, but no wait duration guarantees a natural SPT example. Keep its natural live evidence gate open; a controlled isolated scenario can verify routing without relabeling it as natural SPT evidence or changing the production inputs.

## Verification

- Latest incremental checks after Gemini/capture changes: focused Node suite **12 passed, 0 failed**; isolated PostgreSQL Bridge suite **19 passed, 0 failed**, including capture rotation, expiry, quota, duplicate and no-execution checks. `git diff --check` passed. The full-suite counts below describe the preceding baseline, not a rerun of every suite after this increment.
- Node suite: **123 passed, 0 failed** (includes 12 Bridge core/market/UI tests).
- Full isolated PostgreSQL suite: **45 passed, 0 failed**, including 17 Bridge scenarios. Covers actual receiver/worker fills, duplicate/foreign/unknown targets, stale policy/funding, revisions, old exits, concurrent admission/shutdown, crash rollback, HTTP authentication/CSRF/ownership and backup/restore plus restricted runtime grants.
- Receiver/worker fixtures include 10 SL, 10 TP, 5 both-touched, 5 native/Bridge conflict, 5 rejected and 5 capped cases. Fixture timestamps and market data are synthetic. These establish backend behavior, not TradingView-to-market parity.
- TradingView: an earlier generated v6 fixture compiled and ran on standard BINANCE:BTCUSDT 1D. The wrong-symbol runtime guard was first observed on AAPL; changing to the registered chart removed the runtime error and displayed both EMA plots. No compile diagnostic was visible after successful addition. That fixture had no native alert calls before the Bridge and no alert was created for it. The separate 1-minute transport fixture and alert are documented above.
- Reproducible fixture: `tradingview/fixtures/app3a_indicator.pine`, assembled with `scripts/build-pine-bridge-fixture.mjs NEW_OUTPUT_PATH`. Source SHA-256: `9da68b9dfea1b4863d0573fbf3a153030566ebf94375cdfc31b91663f5cf9f8d`; integrated SHA-256: `e3c5dd0e714142cfa5d193cc6c70c59eb722c343a8ac1eb4655874c1388830d3`.
- The preceding baseline had no live provider delivery or SPT compilation. The staging observations above supersede those gaps, including source-specific TradingView webhook delivery for two 1-minute SPT events. Quantitative multi-bar market parity and production readiness are not yet claimed.

## SPT review and remaining gates

The detailed Spot data-flow audit is in [SPT_SPOT_COMPATIBILITY.md](SPT_SPOT_COMPATIBILITY.md). It finds no direct leverage/margin/funding or contract execution in the submitted indicator, but the source's short simulation, visual state, native `SELL=SHORT` label and cooldown feedback mean the **whole original indicator is not Spot-clean**. A separate Spot review draft compiled on TradingView. Its HTF-off chart baseline differs from the registered original deployment (`useMTF=true` in the stored snapshot); this mismatch blocks reuse of that deployment's evidence. The newer 118-bar trace contains 3 positive EXIT observations matching recalculated history. TradingView still shows a repaint caution and there are no live BUY observations under this baseline, so the draft remains review-only. The existing Bridge capture remains Spot long/EXIT only and evidence-only. New `bridge-exit-v2` artifacts pin Binance Global to the exact `BINANCE:<symbol>` TradingView ticker ID; existing v1 alerts retain their frozen guard and cannot inherit this evidence.

The original hash still matches R-0. Scanner reads all 58 inputs and reports `request.security`, `ta.pivothigh`, `ta.pivotlow` as Quant dependencies. `buySignal` / `sellSignal` are visible globals. Source inspection found one native `alert()` call inside `f_sendNotify`, gated by `notifyEnabled`; TradingView Inputs confirmed `Enable notifications=false` on the saved draft. This supports isolation for `Any alert() function call` in that chart configuration. Any future input or alert-condition change needs a new review. `alertcondition()` declarations do not prove alert isolation.

Source review confirms `rawSellSignal` requires `allowShort` and `sellSignal` represents a confirmed short-entry condition. The Bridge binds that condition as a long-position EXIT; it does not open short positions. The owner explicitly approved this long-only mapping on 2026-09-24: SPT `sellSignal` closes existing Bridge longs and never opens a short. Keep the `Both Long & Short` source direction input so `sellSignal` remains available; changing it to Long only would suppress the signal. The AI system prompt and guide record this rule from `bridge-prompt-v2` / `bridge-guide-v2` onward. Existing staging drafts retain their v1 instruction records; the reviewed mapping and original source behavior are unchanged.

The owner also specified Spot-only Trading Bot and Quant Lab operation. AI instruction versions `bridge-prompt-v3` and `bridge-guide-v3` now require Futures-dependency diagnostics and a separately reviewed Spot-adapted indicator revision before Bridge assembly. This is a new requirement, not a completed adaptation implementation: the current AI worker returns mapping JSON, not revised Pine source. Futures-dependent drafts cannot be treated as Spot execution or Quant-ready. The SPT `sellSignal` mapping approval does not by itself establish complete Spot compatibility of every source dependency; the existing staging artifacts remain tied to their recorded v1 instructions.

The 1-minute source/Bridge audit confirms `buySignal = rawBuySignal and not rawSellSignal` and `sellSignal = rawSellSignal and not rawBuySignal`; both raw conditions require `barstate.isconfirmed`. The appended block reads those globals only after the unchanged source and runs its own closed-bar guard. Its BUY therefore follows the source long signal when ATR/level guards permit; its `NATIVE` EXIT means an opposite short-entry signal for an already tracked Bridge long. A source SELL alone does not create a Bridge short, and a BUY can create a distinct Bridge entry while another Bridge entry remains open. This differs from the source's optional simulated backtest, which tracks a single position and uses its own preview SL and selected TP. The actual captured BUY/TP pair confirms the Bridge's protection path; an observed SPT `sellSignal`/`NATIVE` EXIT is still needed to verify the approved opposite-signal behavior on TradingView.

For repaint review, the four HTF `request.security` calls use `barmerge.lookahead_on` with `[1]` on the expression's close, EMA and Supertrend values, a previous-confirmed-HTF pattern. `ta.pivothigh/ta.pivotlow` use `pivotLen` bars on the right; the script assigns their values to `lastSwingHigh/Low` when they become available, with no negative plot offset found in this source audit. TradingView displayed a repaint caution. The newer HTF-off 118-observation comparison meets the numeric 100/zero-change gate for its measured window, including 3 exits. It does not cover a live BUY or every future input setting; the two earlier captured Bridge events belong to a different snapshot.

Source presets override several numeric inputs unless `preset=Custom`. Source SL/RR inputs (`slAtrBufferInput`, `minRiskATRInput`, `rr1`–`rr5`) remain separate from the Bridge pair. Their relevance must be reviewed; the scanner's 20 numeric candidates are not an approved optimization list. Visual input `lineForwardBars` must not become an optimization dimension.

The initial byte-bound budget rejected SPT. The OpenAI adapter's pinned tokenizer and compact metadata admit the full original analysis at **21,745 reserved input tokens**, including framing; this is a local count, not a Gemini measurement. Gemini's separate counted AI-only view measured 22,885 input tokens in the successful analysis. Source lines over 4,096 UTF-8 bytes are rejected before tokenization (`SOURCE_LINE_TOO_LONG`) to bound pathological long-literal processing. An initial stress test exposed this delay and caused older timestamp-based tests to expire; the final regression is after adding this resource guard.

The 2026-09-26 [acceptance checkpoint](APP_3A_ACCEPTANCE_2026-09-26.md) supersedes this record's earlier pending registration/compilation checklist. Spot revision 4 is registered and compiled. A read-only audit verified first protection touch, close/ATR and exact rounded levels for its 11 captured exits with no gaps or mismatches. The v2 isolated HTTP/Paper NATIVE route and bounded wait/expiry tests passed; a natural SPT NATIVE event is not an additional prerequisite for these controlled Bridge checks.

The hosted v2 Paper timing and readiness-manifest checks are now complete. A separate test Bot/database reused the exact compiled v4 artifact with a rebuilt canary snapshot. Independently collected Spot bars supported controlled HTTP BUY and targeted NATIVE EXIT, with receipt-to-queue observations of 474/450 ms, 2 fills and no duplicate effects. A 5,000 ms deadline expired with 0 signals/fills and no revival by a late bar. Restricted API/worker roles could not write readiness/market tables. Operator recording and owner activation were exercised only in that replica, which was stopped and removed afterward. The actual TradingView SPT deployment remains DRAFT/capture-only with 0 execution events. Fee/slippage/risk were 10 bps/1 bp/1%; the test used a 300-second age limit for recent-bar replay. A 10 bps slippage rehearsal was correctly rejected because its BUY price exceeded TP. Preserve that limitation and unknown provider outcomes without retrying them.

QL-2A owns actual-source evaluator coverage, state initialization, numeric optimization bounds, historical sample floors and repaint/evaluator parity. Those checks do not block Bridge draft delivery or supported isolated Paper testing. SPT remains Quant unsupported and its older trace evidence cannot silently be attached to a different source/artifact/snapshot. QL-2A can now start with that explicit unsupported status; this is permission to build and prove the evaluator, not to optimize SPT already. SMTP 550 remains an independent Email Report delivery gate.
